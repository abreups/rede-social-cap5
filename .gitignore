# ignore todos os arquivos no diretorio node_modules
node_modules/

# ignore todos os arquivos no diretorio .git (necessário?)
.git/

# ignore todos os arquivos que terminem em ~ (temp do vim)
*.*~

